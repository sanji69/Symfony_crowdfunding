{% extends 'base.html.twig' %}

{% block title %}Posts{% endblock %}

{% block body %}

    {{ form(form) }}


    <table class="table table-striped">
        <thead>
            <tr>
                <th>Titre</th>
                <th>Publié ?</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            {% for post in posts %}
                <tr>
                    <td>{{ post.title }}</td>
                    <td>{{ post.published }}</td>
                    <td></td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
