{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div>
        <h2>{{ article.title }}</h2>

        <div>{{ article.content }}</div>
        <a href="{{ path('articles/retrieve.html.twig', {
            "id":article.id
        }) }}">Modifier l'aricle</a>
    </div>




{% endblock %}
