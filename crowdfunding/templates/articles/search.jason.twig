
[{% for article in articles -%}
    {{ {id: article.id, label: article.name, value: article.name}|json_encode|raw }}
    {# use "value" instead of "id" key, if you use jquery-ui #}
    {%- if not loop.last %},{% endif -%}
{%- endfor %}]